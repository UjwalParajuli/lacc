<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".BookmarkActivity">

    <include
        android:id="@+id/toolbar_bookmark"
        layout="@layout/toolbar_bookmark" />

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recycler_view_bookmark"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintEnd_toEndOf="@id/toolbar_bookmark"
        app:layout_constraintStart_toStartOf="@id/toolbar_bookmark"
        app:layout_constraintTop_toBottomOf="@+id/toolbar_bookmark"
        />

</androidx.constraintlayout.widget.ConstraintLayout>